server:
  shutdown: immediate

spring:
  datasource:
    url: 'jdbc:h2:mem:hmpps-workload;INIT=create domain if not exists jsonb as text;MODE=PostgreSQL;'
    username: probation
    password: tiering
management.endpoint:
  health.cache.time-to-live: 0
  info.cache.time-to-live: 0
